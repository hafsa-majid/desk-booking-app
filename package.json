{
  "name": "desk-booking-app",
  "version": "1.0.0",
  "description": "Production-grade desk booking system with CI/CD, testing, and security scanning",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint .",
    "format:check": "prettier --check .",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "mkdir -p build && cp package*.json build/ && cp server.js build/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "helmet": "^7.2.0"
  },
  "devDependencies": {
    "@cyclonedx/cyclonedx-npm": "^1.20.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "prettier": "^3.0.3",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "Hafsa Majid",
  "license": "MIT"
}